[default]
    locale = "en-us"

    [default.extend-words]
        # curren = "curren" # Used in the context of '¤100'
        # nd     = "nd"     # Used in the context of '2nd'

[files]
    extend-exclude = ["*_cache", ".env", ".env.secrets", ".venv", "CHANGELOG.md", "static"]
